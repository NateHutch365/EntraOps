---
phase: 06-settings-polish
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: false
requirements: [SETT-01, SETT-02, SETT-03]

must_haves:
  truths:
    - "All Phase 6 features visually verified by human in browser"
    - "Settings page loads, displays structured form, allows editing and saving"
    - "Empty state works when no config exists"
    - "Terminal output line spacing is correct"
    - "Sidebar has all 7 entries in correct order"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 6 work: Settings page (structured form, edit mode, save flow, empty state) and cross-cutting polish fixes (terminal spacing, sidebar nav).

Purpose: Final validation before closing Phase 6 and the v1.0 milestone.
Output: Human approval or issue list for remediation.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-settings-polish/06-CONTEXT.md
@.planning/phases/06-settings-polish/06-01-SUMMARY.md
@.planning/phases/06-settings-polish/06-02-SUMMARY.md
@.planning/phases/06-settings-polish/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 6 Settings & Polish end-to-end</name>
  <files>gui/client/src/pages/SettingsPage.tsx</files>
  <action>
    Run `cd gui && npm run dev` to start the dev server. Then verify all Phase 6 features in the browser.

    **Settings Feature (SETT-01, SETT-02, SETT-03):**
    - Structured settings page at /settings with 5 section cards
    - View-only default mode with "Edit Settings" button
    - TenantId/TenantName read-only with lock icon
    - Toggle-controlled sub-fields (disabled and greyed when toggle off)
    - Cron picker with 5 select dropdowns and preview
    - DiffDialog showing before/after of full config JSON
    - Zod validation blocking DiffDialog on invalid data
    - SaveBanner reminding to commit to git
    - Empty state with mini-form generating PowerShell command
    - "Check again" re-fetch

    **Polish Fixes:**
    - Terminal output line spacing fixed (leading-normal)
    - \r stripping in ConnectPage and RunCommandsPage
    - Sidebar nav: 7 entries in correct order with Settings entry
  </action>
  <verify>
    Human verification steps:

    Start dev server: `cd gui && npm run dev`

    **1. Settings page — SETT-01 (structured display):**
    - Navigate to /settings from sidebar
    - Verify sidebar "Settings" entry is present and highlighted
    - If EntraOpsConfig.json exists: verify 5 section cards visible (Identity and Auth, Automation, Integrations, AD Management, Security Attributes)
    - Verify TenantId and TenantName show as greyed read-only text with lock icon
    - Verify all other fields are read-only (non-interactive) by default

    **2. Settings page — SETT-02 (edit and save):**
    - Click "Edit Settings" button
    - Toggle a boolean switch (e.g. IngestToLogAnalytics) — verify sub-fields grey out when off, light up when on
    - Edit a text field — verify "Unsaved changes" amber badge appears in sticky header
    - Click "Preview Changes" — verify DiffDialog shows with green/red diff lines
    - Confirm save — verify SaveBanner appears ("Changes saved — remember to commit...")
    - Verify file on disk is updated: cat EntraOpsConfig.json | head -5

    **3. Settings page — SETT-03 (empty state):**
    - Rename config: mv EntraOpsConfig.json EntraOpsConfig.json.bak
    - Refresh /settings page
    - Verify empty state shows with "No Configuration File" message
    - Type a tenant name — verify PowerShell command updates live
    - Click copy icon — verify clipboard contains the command
    - Click "Show advanced options" — verify additional inputs appear and command updates
    - Click "Check again" — verify page re-fetches (will still show empty state until file exists)
    - Restore: mv EntraOpsConfig.json.bak EntraOpsConfig.json

    **4. Cron picker:**
    - In edit mode, find a cron field (WorkflowTrigger section)
    - Verify 5 dropdown selects visible
    - Change minute/hour dropdowns — verify preview text updates below

    **5. Terminal line spacing:**
    - Navigate to /run (Run Commands page)
    - Run any available command
    - Verify output lines have normal spacing (not double-spaced)

    **6. Sidebar nav audit:**
    - Verify 7 entries in order: Dashboard, Browse Objects, Templates, Run Commands, Connect, History, Settings
    - Click each — verify correct page loads and NavLink active state highlights
  </verify>
  <done>All 6 verification steps pass. Settings page works end-to-end (display, edit, save, empty state). Polish fixes confirmed (terminal spacing, sidebar nav). Phase 6 complete.</done>
</task>

</tasks>

<verification>
Human approval of all 6 verification steps above.
</verification>

<success_criteria>
- All Phase 6 success criteria from ROADMAP.md met:
  1. User can view and edit EntraOpsConfig.json in a structured form; changes validated and saved safely
  2. When no config file exists, Settings page shows clear empty state with New-EntraOpsConfigFile instructions — no crash, no blank page
- Terminal line spacing fixed
- Sidebar nav complete with 7 entries
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-polish/06-04-SUMMARY.md`
</output>
