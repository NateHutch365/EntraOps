---
phase: 02-classification-template-editor
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-03-25T15:56:28.293Z
---

<current_state>
Phase 2 (Classification Template Editor) is underway. Plans 02-01 and 02-02 are both complete and committed. The /templates page exists with working read-only accordion browsing. Pausing after 02-02 — next up is 02-03 (chip editor + save flow).

02-01 delivered: TypeScript types in gui/shared/types/templates.ts + Express /api/templates/* routes (GET all, GET global, PUT update).
02-02 delivered: TemplatesPage (6-tab), TierAccordion (read-only entry cards), DiffDialog (green/red diff preview), sidebar nav + React Router route.
</current_state>

<completed_work>

## Phase 2: Classification Template Editor — 2 of 5 plans complete

### Plan 02-01 — COMPLETE
- gui/shared/types/templates.ts — TemplateName, TierBlock, TierLevelDefinitionEntry, GetTemplateResponse, GetGlobalResponse
- gui/server/routes/templates.ts — GET /api/templates/:name, GET /api/templates/global, PUT /api/templates/:name, PUT /api/templates/global
- Atomic write (tmp → rename), BOM stripping, Zod v4 validation, path-traversal guard (assertSafePath)
- /global registered BEFORE /:name to prevent param capture

### Plan 02-02 — COMPLETE (just finished this session)
- gui/client/src/pages/TemplatesPage.tsx — 6-tab page (5 TemplateName tabs + Global Exclusions placeholder). Fetches /api/templates/:name on tab activation, caches per-tab, skips 'global' tab.
- gui/client/src/components/templates/TierAccordion.tsx — read-only accordion; tier semantic color classes (text-tier-control, etc.); entry cards with scope Badge + action pills
- gui/client/src/components/templates/DiffDialog.tsx — diffLines() from 'diff' package; bg-green-950/bg-red-950 for added/removed lines inside ScrollArea
- gui/client/src/components/layout/Sidebar.tsx — FileJson icon + /templates NavLink added to NAV_ITEMS
- gui/client/src/App.tsx — <Route path="templates" element={<TemplatesPage />} /> added
- shadcn accordion, tabs, alert components installed; diff + @types/diff installed
- Pre-existing build fixes: created vite-env.d.ts (missing from Phase 1 scaffold) + installed @testing-library/jest-dom
- TypeScript build: ✓ passes cleanly (tsc -b && vite build, 1.68s)

Commits: ffcf85c (Task 1), 07ae9b1 (Task 2), 5f6162b (docs)
</completed_work>

<remaining_work>

## Plans remaining in Phase 2

### Plan 02-03 — NOT STARTED (Wave 3)
Goal: Add interactive editing to TierAccordion. Each entry card gets:
- ChipEditor component (gui/client/src/components/templates/ChipEditor.tsx) — controlled pill chips for RoleDefinitionActions, per-chip × dismiss, Enter/Add button to add new action
- TierAccordion updated to accept onSaveEntry prop, show ChipEditor per entry, "Preview Changes" button
- TemplatesPage updated: preview state, diff state, PUT /api/templates/:name call on Confirm
- DiffDialog (already built in 02-02) wired to the save flow
- Zod validation before opening DiffDialog (inline error if invalid)

### Plan 02-04 — NOT STARTED (Wave 4)
Goal: Global Exclusions tab editor + post-save git-commit warning banner
- GlobalExclusionsTab.tsx — chip editor for exclusion string[], fetch/PUT /api/templates/global, DiffDialog wired
- SaveBanner.tsx — info banner shown after any successful save: "Remember to git commit your changes"
- TemplatesPage global tab wired to GlobalExclusionsTab, SaveBanner shown after save

### Plan 02-05 — NOT STARTED (Wave 5, checkpoint)
Goal: Human browser verification of complete template editor (autonomous: false)
- All 4 Phase 2 success criteria tested end-to-end in browser

## Key interfaces for 02-03

From gui/shared/types/templates.ts:
- TemplateName, TierBlock, TierLevelDefinitionEntry (Category, Service, RoleAssignmentScopeName[], RoleDefinitionActions[])
- PUT /api/templates/:name expects { tiers: TierBlock[] }, returns { name, tiers }

From gui/client/src/components/templates/DiffDialog.tsx (already built):
- Props: { open, title, before, after, onConfirm, onCancel, loading? }
- before/after are JSON.stringify(data, null, 2)
</remaining_work>

<decisions_made>

- **shared types import path**: relative paths (e.g. ../../../../shared/types/templates) — no tsconfig path alias for shared yet
- **TierAccordion read-only in 02-02**: onSaveEntry prop intentionally omitted; 02-03 adds it without breaking the read-only shell
- **DiffDialog built in 02-02 not 02-03**: so it's available to both 02-03 and 02-04 without a dependency chain
- **activeTab type is TemplateName | 'global'**: fetch effect has early-return guard for 'global'
- **Tab data caching**: if templateData[name] already populated, skip fetch — prevents redundant API calls on re-activation
- **Tier semantic colors**: TIER_CLASS constant maps EAMTierLevelName → text-tier-* Tailwind class (from globals.css @theme)
- **Pre-existing build fixes committed in 02-02**: vite-env.d.ts and @testing-library/jest-dom — documented as deviations in SUMMARY
</decisions_made>

<blockers>
None. Build passes, API complete, UI shell complete. Clean slate for 02-03.
</blockers>

<next_action>
Resume with: /gsd:execute-phase 2 --plan 02-03

This will execute the chip editor + save flow plan (ChipEditor.tsx + TierAccordion edit mode + TemplatesPage PUT wiring).
/clear first for a fresh context window.
</next_action>
