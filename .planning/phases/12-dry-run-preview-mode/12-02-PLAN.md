---
phase: 12-dry-run-preview-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/server/services/commands.ts
autonomous: false
requirements:
  - IMPL-05
must_haves:
  truths:
    - "Dry-run runs are excluded from run history"
    - "Live runs continue to be recorded in run history"
  artifacts:
    - path: "gui/server/services/commands.ts"
      provides: "SampleMode guard before appendHistory call"
      contains: "SampleMode"
  key_links:
    - from: "proc.on('close') callback"
      to: "appendHistory(record)"
      via: "if (!parameters.SampleMode) guard"
      pattern: "SampleMode.*appendHistory|appendHistory.*SampleMode"
---

<objective>
Add a guard in the server `runCommand()` function to skip run history recording when `parameters.SampleMode` is true, then verify the complete dry-run flow end-to-end.

Purpose: Dry-run simulations should not pollute run history since they write nothing to Entra — there is nothing to audit.
Output: Modified commands.ts with history exclusion guard; human-verified dry-run flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dry-run-preview-mode/12-CONTEXT.md

<interfaces>
<!-- Server file context for executor -->

From gui/server/services/commands.ts (line ~170-190, inside proc.on('close') callback):
```typescript
const record: RunHistoryRecord = {
  id: runId,
  cmdlet,
  parameters,
  startedAt,
  endedAt,
  durationSeconds,
  outcome,
};

// Write history async — do not block stream close
appendHistory(record).catch((err) => {
  console.error('Failed to write run history:', err);
});
```

The `parameters` variable is the CmdletParameters passed to `runCommand()`. When dry-run is active, `parameters.SampleMode === true`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SampleMode guard to skip history for dry-run runs</name>
  <files>gui/server/services/commands.ts</files>
  <read_first>
    - gui/server/services/commands.ts (read fully — ~200 lines)
    - gui/shared/types/commands.ts (CmdletParameters type — confirm SampleMode field)
    - .planning/phases/12-dry-run-preview-mode/12-CONTEXT.md (decisions D-05 and D-06)
  </read_first>
  <action>
In `gui/server/services/commands.ts`, inside the `proc.on('close')` callback, wrap the `appendHistory(record)` call with a SampleMode guard. Per D-05 and D-06:

Change from:
```typescript
// Write history async — do not block stream close
appendHistory(record).catch((err) => {
  console.error('Failed to write run history:', err);
});
```

To:
```typescript
// Write history async — do not block stream close
// Skip history for dry-run (SampleMode) runs — nothing was written to Entra, nothing to audit
if (!parameters.SampleMode) {
  appendHistory(record).catch((err) => {
    console.error('Failed to write run history:', err);
  });
}
```

This is the only server change in Phase 12. No type changes, no API changes, no new imports.
  </action>
  <acceptance_criteria>
    - gui/server/services/commands.ts contains `if (!parameters.SampleMode)` before `appendHistory(record)`
    - The `appendHistory(record)` call is inside the `if` block, not outside it
    - No other changes to commands.ts beyond this guard
    - `npx tsc --noEmit` passes with zero errors (run from gui/)
  </acceptance_criteria>
  <verify>
    <automated>cd gui && npx tsc --noEmit 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <done>History exclusion guard added — dry-run runs skip appendHistory, live runs continue recording normally. TypeScript compiles with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete dry-run flow end-to-end</name>
  <files>gui/client/src/pages/ApplyPage.tsx, gui/server/services/commands.ts</files>
  <action>Human verification — no automated changes. Review the complete dry-run flow in the browser.</action>
  <what-built>
Complete dry-run/preview mode: toggle on Screen 1, simulation alerts on Screen 2, simulating badges + [DRY RUN] separators on Screen 3, sky outcome banners + sim badges on Screen 4, SampleMode parameter passthrough, and run history exclusion for dry-run runs.
  </what-built>
  <how-to-verify>
1. Start the dev server if not running: `cd gui && npm run dev`
2. Open http://localhost:5173/apply
3. **Screen 1 — Toggle:** Verify the dry-run toggle appears between action cards and the CTA button. Toggle it ON — confirm "◈ Simulation active" sky badge appears and CTA changes to "Review &amp; Simulate"
4. **Screen 1 — Toggle OFF:** Toggle OFF — badge disappears, CTA reverts to "Review &amp; Apply"
5. **Screen 2 — Dry-run confirmation:** Enable dry-run, click "Review &amp; Simulate". Verify:
   - Sky-colored Alert with FlaskConical icon and "Simulation Mode" title
   - Parameters column shows "-SampleMode" (or "tenantName -SampleMode" if configured)
   - Subtitle reads "...will simulate in order..."
   - CTA reads "Simulate Now"
6. **Screen 2 — Live confirmation:** Go back, disable dry-run, click "Review &amp; Apply". Verify amber alert and "Run Now" CTA are shown (unchanged from Phase 11)
7. **(Optional) Screen 3 — Running:** If PowerShell is available, start a dry-run. Verify sky "◈ Simulating" badge, "Simulating — Dry-run in progress…" heading, sky reminder banner above terminal, and "[DRY RUN]" prefix in terminal separators
8. **(Optional) Screen 4 — Outcome:** After run completes, verify sky "◈ Simulated" badge, "Dry-run Complete — No changes made" heading, sky banner, and "◈ Sim. Pass" per-cmdlet badges
9. **Apply Again:** Click "Apply Again" — verify dry-run toggle is still ON (isDryRun preserved)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>
    <automated>cd gui && npx tsc --noEmit 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <done>Human verified dry-run flow in browser — toggle, confirmation, execution, and outcome screens all show correct dry-run visual treatment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | SampleMode boolean in POST body controls history skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | POST body SampleMode field | accept | A client could send SampleMode=true to avoid history logging. This is acceptable: the tool is single-user local, history is informational not security-critical, and the admin is the only user. No escalation path — SampleMode only affects history recording and cmdlet behavior. |
| T-12-04 | Repudiation | Skipped history for dry-runs | accept | Dry-runs write nothing to Entra — there are no changes to audit. Skipping history is the correct behavior, not a gap. |
</threat_model>

<verification>
- TypeScript compiles: `cd gui && npx tsc --noEmit`
- SampleMode guard present before appendHistory
- Human verifies dry-run flow end-to-end in browser
</verification>

<success_criteria>
Dry-run runs are excluded from run history. The complete dry-run flow is verified in the browser — toggle, confirmation, (optionally) execution, and outcome screens all show correct dry-run visual treatment. Admin cannot mistake a dry-run for a real run.
</success_criteria>

<output>
After completion, create `.planning/phases/12-dry-run-preview-mode/12-02-SUMMARY.md`
</output>
