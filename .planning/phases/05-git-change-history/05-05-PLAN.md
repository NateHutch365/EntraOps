---
phase: 05-git-change-history
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified: []
autonomous: false
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]
must_haves:
  truths:
    - "End-to-end flow works: History page → expand commit → see changes → select 2 → compare"
    - "Empty state renders correctly when no PrivilegedEAM commits exist"
    - "ControlPlane section is visually distinct with blue border and tint"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete History feature.

Purpose: Validate the end-to-end flow before marking Phase 5 complete.
Output: User confirmation that the feature works as expected.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-git-change-history/05-CONTEXT.md
@.planning/phases/05-git-change-history/05-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete History feature: paginated commit list with expandable rows showing structured change summaries, RBAC filter chips, 2-commit selection with sticky compare bar, and Compare page with side-by-side metadata + tabbed structured diff + raw diff viewer. Sidebar nav entry added.
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd gui && npm run dev`
2. Open the app in browser — confirm "History" appears in sidebar nav
3. Click "History" — confirm you see the Change History page
4. **If no commits touch PrivilegedEAM/:**
   - Confirm the empty state message appears: "No change history yet" with instructions to run `Save-EntraOpsPrivilegedEAMJson`
   - To test with data: commit the `PrivilegedEAM/` folder (`git add PrivilegedEAM/ && git commit -m "initial EAM data"`), then make a change and commit again. Refresh the History page.
5. **With commits available:**
   - Confirm commit list shows hash, message, author, date
   - Confirm pagination controls work (if >20 commits)
   - Click a commit row — confirm it expands inline
   - If commit has PrivilegedEAM changes: confirm RBAC system tabs appear with structured change summary
   - Confirm ControlPlane section has visually distinct blue-tinted left border and faint background
   - If commit has no PrivilegedEAM changes: confirm "No PrivilegedEAM/ changes in this commit" message
6. **RBAC Filter:** Click the RBAC System filter above the list — select a system — confirm only commits affecting that system are shown
7. **Compare flow:** Check 2 commit checkboxes → confirm sticky compare bar appears at bottom → click "Compare Commits"
8. **Compare page:** Confirm side-by-side "From" and "To" header, RBAC system tabs with structured diff, and raw diff with green/red line coloring
9. Click "Back to History" — confirm you return to the History page
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
User confirms end-to-end History feature works correctly.
</verification>

<success_criteria>
User approves the History and Compare pages work as specified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-git-change-history/05-05-SUMMARY.md`
</output>
