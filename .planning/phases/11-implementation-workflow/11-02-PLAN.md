---
phase: 11-implementation-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - gui/client/src/pages/ObjectBrowser.tsx
  - gui/client/src/pages/ReclassifyPage.tsx
autonomous: false
requirements:
  - IMPL-02

must_haves:
  truths:
    - "Object Browser page shows an 'Apply to Entra' button that navigates to /apply"
    - "Reclassify page shows an 'Apply to Entra' button that navigates to /apply"
    - "Full workflow is verifiable end-to-end from sidebar, CTAs, and all 4 page states"
  artifacts:
    - path: "gui/client/src/pages/ObjectBrowser.tsx"
      provides: "Apply to Entra CTA button in page header"
      contains: "Apply to Entra"
    - path: "gui/client/src/pages/ReclassifyPage.tsx"
      provides: "Apply to Entra CTA button in page header"
      contains: "Apply to Entra"
  key_links:
    - from: "gui/client/src/pages/ObjectBrowser.tsx"
      to: "/apply"
      via: "navigate('/apply')"
      pattern: "navigate.*apply"
    - from: "gui/client/src/pages/ReclassifyPage.tsx"
      to: "/apply"
      via: "navigate('/apply')"
      pattern: "navigate.*apply"
---

<objective>
Add "Apply to Entra" call-to-action buttons to the Object Browser and Reclassify pages header areas, and verify the full implementation workflow end-to-end.

Purpose: Delivers IMPL-02 (reachability from existing screens) and validates the complete Phase 11 user journey.

Output: CTA buttons on both pages, human-verified workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-implementation-workflow/11-CONTEXT.md
@.planning/phases/11-implementation-workflow/11-UI-SPEC.md
@.planning/phases/11-implementation-workflow/11-01-SUMMARY.md

<interfaces>
<!-- CTA placement context from existing pages -->

From gui/client/src/pages/ObjectBrowser.tsx (header area, lines 88-95):
```tsx
return (
  <div className="p-6 space-y-4">
    <div>
      <h1 className="text-[22px] font-semibold">Object Browser</h1>
      <p className="text-sm text-muted-foreground">
        Browse and filter privileged identity objects across all RBAC systems
      </p>
    </div>
    <!-- ObjectFilters follows -->
```

From gui/client/src/pages/ReclassifyPage.tsx (header area, lines 187-194):
```tsx
return (
  <div className="flex flex-col h-full">
    <div className="px-6 py-4 border-b border-border">
      <h1 className="text-xl font-semibold">Reclassify Objects</h1>
      <p className="text-sm text-muted-foreground mt-1">
        Override the tier assignment for individual objects. Changes are display-layer only.
      </p>
    </div>
```

Both pages already import `useNavigate` from 'react-router'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Apply to Entra" CTA to ObjectBrowser and ReclassifyPage (IMPL-02)</name>
  <files>gui/client/src/pages/ObjectBrowser.tsx, gui/client/src/pages/ReclassifyPage.tsx</files>
  <read_first>
    gui/client/src/pages/ObjectBrowser.tsx
    gui/client/src/pages/ReclassifyPage.tsx
  </read_first>
  <action>
**ObjectBrowser.tsx:**
1. Add `PlayCircle` to the lucide-react imports (if not present).
2. Modify the header `<div>` that wraps H1 + subtitle. Convert it to a `flex justify-between items-start` container:
   ```tsx
   <div className="flex items-start justify-between">
     <div>
       <h1 className="text-[22px] font-semibold">Object Browser</h1>
       <p className="text-sm text-muted-foreground">
         Browse and filter privileged identity objects across all RBAC systems
       </p>
     </div>
     <Button variant="secondary" onClick={() => navigate('/apply')}>
       <PlayCircle size={16} className="mr-2" />
       Apply to Entra
     </Button>
   </div>
   ```
3. Add `Button` to the imports from `@/components/ui/button` (if not already imported).

**ReclassifyPage.tsx:**
1. Add `PlayCircle` to the lucide-react imports.
2. Modify the header `<div className="px-6 py-4 border-b border-border">` to include the CTA. Add a flex wrapper:
   ```tsx
   <div className="px-6 py-4 border-b border-border">
     <div className="flex items-start justify-between">
       <div>
         <h1 className="text-xl font-semibold">Reclassify Objects</h1>
         <p className="text-sm text-muted-foreground mt-1">
           Override the tier assignment for individual objects. Changes are display-layer only.
         </p>
       </div>
       <Button variant="secondary" onClick={() => navigate('/apply')}>
         <PlayCircle size={16} className="mr-2" />
         Apply to Entra
       </Button>
     </div>
   </div>
   ```
3. Add `Button` to imports from `@/components/ui/button` (if not already imported).

Both CTAs are page-level header buttons (not row-level actions). They use `variant="secondary"` with `PlayCircle` icon per UI-SPEC cross-page CTA spec. Both use the existing `navigate` from `useNavigate()` already present in both files.
  </action>
  <acceptance_criteria>
    - ObjectBrowser.tsx contains `Apply to Entra` string
    - ObjectBrowser.tsx contains `navigate('/apply')` or `navigate("/apply")`
    - ObjectBrowser.tsx contains `PlayCircle` import from lucide-react
    - ReclassifyPage.tsx contains `Apply to Entra` string
    - ReclassifyPage.tsx contains `navigate('/apply')` or `navigate("/apply")`
    - ReclassifyPage.tsx contains `PlayCircle` import from lucide-react
    - Both files contain `variant="secondary"` on the CTA button
  </acceptance_criteria>
  <verify>
    <automated>cd gui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both Object Browser and Reclassify pages show an "Apply to Entra" secondary button in the page header. Clicking navigates to /apply. TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify full implementation workflow</name>
  <what-built>
    Complete "Apply to Entra" implementation workflow:
    - Sidebar nav entry with PlayCircle icon
    - CTA buttons on Object Browser and Reclassify pages
    - Action selection screen with 4 checkbox cards (all checked by default)
    - Pre-run confirmation with warning alert and cmdlet review table
    - Live SSE streaming terminal output with sequential cmdlet execution
    - Outcome summary with pass/fail badges per cmdlet
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd gui && npm run dev`
    2. Open browser to http://localhost:5173
    3. Verify sidebar shows "Apply to Entra" link with PlayCircle icon (between Exclusions and Templates)
    4. Click "Apply to Entra" — verify action selection page with 4 checkbox cards, all checked
    5. Navigate to Browse Objects — verify "Apply to Entra" secondary button in header
    6. Click the CTA — verify it navigates to /apply
    7. Navigate to Reclassify — verify "Apply to Entra" secondary button in header
    8. Return to /apply, uncheck one action, click "Review & Apply"
    9. Verify confirmation screen: amber warning alert, cmdlet table with 3 rows, Back and Run Now buttons
    10. Click "Back" — verify returns to action selection with same selections preserved
    11. Click "Review & Apply" again, then "Run Now"
    12. Verify live SSE output streams in terminal with separators between cmdlets
    13. After completion, verify outcome summary with per-cmdlet pass/fail badges
    14. Click "Apply Again" — verify returns to action selection
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /apply route | Navigation from existing pages to new Apply page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Spoofing | CTA navigate() | accept | Client-side navigation only, no server-side data — /apply route is public within local app |
</threat_model>

<verification>
- `cd gui && npx tsc --noEmit` passes with zero errors
- ObjectBrowser.tsx has "Apply to Entra" CTA
- ReclassifyPage.tsx has "Apply to Entra" CTA
- Full workflow visually verified in browser
</verification>

<success_criteria>
User can reach /apply from Object Browser CTA, Reclassify CTA, and sidebar link. Full workflow (select → confirm → run → outcome) works end-to-end with live SSE streaming.
</success_criteria>

<output>
After completion, create `.planning/phases/11-implementation-workflow/11-02-SUMMARY.md`
</output>
